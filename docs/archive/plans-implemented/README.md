# Planes Implementados (Archivo)

Documentación histórica preservada solo por trazabilidad. **No operativa** — no usar como guía para el desarrollo actual; el estado vigente vive en el código y en [CLAUDE.md](../../../CLAUDE.md).

Todos estos planes y diseños fueron **ejecutados y completados**: el código correspondiente (tablas DDL, endpoints, componentes UI, tests) ya existe en el repositorio. Los documentos `-design.md` quedaron superados por el código entregado.

## Contenido

| Carpeta | Origen | Contenido |
|---------|--------|-----------|
| `plans/` | `docs/plans/` | 23 planes + diseños por dominio: SISREC 8-fases, clasificador presupuestario, tablas paramétricas, parentesco 8%, admisibilidad, certificación C33, waves UX 2-5. |
| `superpowers/` | `docs/superpowers/` | Features avanzadas implementadas: nuevos roles en ciclo IPR, refresh visual por capas, journey inbox, ejecutor journey-first, rediseño detalle IPR, bug capturer. |

> **Eliminado**: el plan `ssot-gore-nuble` (y su spec) fue removido — apuntaba a un repositorio externo (`gorenuble`) y no trazaba ningún componente de GORE_OS.

Para los índices originales con estado de implementación por documento, ver los `README.md` dentro de cada subcarpeta.
